cd server                          ( direct to server )
venv\Scripts\activate              ( activate the environment )
uvicorn app.main:app --reload      ( start the server )
deactivate                         ( deactivate virtual environment )

